# Cyber Risk Assessment Dashboard (Flask)

## Setup (macOS / Linux / Windows)

1. Clone project and place your Excel file at the path used in `app.py` or update EXCEL_PATH.

2. Create virtualenv and activate:

```bash
python3 -m venv venv
source venv/bin/activate # macOS / Linux
venv\Scripts\activate # Windows PowerShell